#drop n create table again, good for testing, comment this in production RecentlyUsedApiUsage
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driver-class-name=${DB_DRIVER}
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASS}
server.port=${PORT:8080}
server.servlet.context-path=/api/ver1
security.secret.pbkdf2=${PASSWORD_HASH_SECRET:change-weak-secret}
security.secret.jwt=${JWT_SECRET:xZNEEuk91OgleeDllowBLt0F48HPVh/zfRoXsRgB0Lc=}
security.jwt.valid.duration=${JWT_VALID_DURATION:PT24H}
security.password.policy=${PASSWORD_POLICY:classpath:/password-policy.yaml}
recaptcha.secret.key=${RECAPTCHA_SECRET_KEY}
mail.from=${MAIL_FROM:noreply@developer-portal.notam.faa.gov}
mail.feedback=${MAIL_FEEDBACK:support@developer-portal.notam.faa.gov}
mail.templates.path=${MAIL_TEMPLATES_PATH:classpath:/templates/email/}
pagination.limit.default=${PAGINATION_LIMIT_DEFAULT:20}
pagination.limit.max=${PAGINATION_LIMIT_MAX:1000}
